
  const fetchDevelopers = (teamMembers) => {
    let arr = empData.employees.filter((emp, index) => teamMembers.includes(emp.empID))
    console.log("arr:", arr);

    return arr;
  }

  const clickHandler = (emp) => {
    setOpen(!open);
    setSelectedEmp(emp);
    <EmployeeModal selectedEmp={ selectedEmp } open={ open } setOpen={ setOpen } />
  }

  const seeDevelop = (project) => {
    console.log("see develop")
    let filteredEmp = fetchDevelopers(project.teamMembers)
    // navigate("/projects/developers",  )
    return <EmployeeCard filteredEmp={ filteredEmp } />
  }